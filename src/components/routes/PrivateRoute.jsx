import React, { createContext } from 'react';

const AuthContext = createContext(nu)
const PrivateRoute = () => {
    return (
        <div>
            
        </div>
    );
};

export default PrivateRoute;